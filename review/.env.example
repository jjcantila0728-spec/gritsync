VITE_API_BASE=https://gritsync.com/api
VITE_MAIN_SITE=https://gritsync.com
# Stripe publishable key — used as a fallback when the server-side plan config
# doesn't expose one via /api/nclex/plans. Use pk_test_... for non-prod.
VITE_STRIPE_PUBLISHABLE_KEY=
