# Build artifacts and local dev
node_modules
dist
.env
.env.*
!.env.example

# Dev/admin scripts — not needed in the deployed lambda, but the
# build-server bundle script IS needed during build.
scripts/*
!scripts/build-server.cjs

# Docker / Replit / local tooling
docker*
Docker*
.dockerignore
.replit
replit.nix
.local/

# Test output
coverage/
*.test.ts
*.spec.ts
__tests__/

# Editor and OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/
